import styled from 'styled-components';

const StyledWrapper = styled.div`

`;

export default function User() {
    return (
        <StyledWrapper>
            Autenticado
        </StyledWrapper>
    )
}